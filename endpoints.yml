action_endpoint:
  url: "http://localhost:5055/webhook"

model_groups:
  - id: openai_llm
    models:
      - model: "gpt-4o-2024-11-20"
        provider: "openai"
        api_key: ${OPENAI_API_KEY}
        timeout: 7
        temperature: 0.0